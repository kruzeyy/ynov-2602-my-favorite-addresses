info:
  name: Test
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/users/
  body:
    type: json
    data: |-
      {
        "email" : "pontus@gmail.com",
        "password" : "test"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
